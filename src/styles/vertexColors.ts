const defaultVertex = 'fill-slate-400'
const lookedVertex = 'fill-red-200'
const chosenVertex = 'fill-yellow-700'
const visitedVertex = 'fill-red-700'

const colors = {
  defaultVertex,
  lookedVertex,
  chosenVertex,
  visitedVertex,
}

const colorsList = Object.values(colors)

export default {
  colors,
  colorsList,
}

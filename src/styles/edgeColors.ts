const defaultEdge = 'stroke-slate-400'
const lookedEdge = 'fill-red-200'
const chosenEdge = 'fill-yellow-700'
const visitedEdge = 'fill-red-700'

const colors = {
  defaultEdge,
  lookedEdge,
  chosenEdge,
  visitedEdge,
}

const colorsList = Object.values(colors)

export default {
  colors,
  colorsList,
}
